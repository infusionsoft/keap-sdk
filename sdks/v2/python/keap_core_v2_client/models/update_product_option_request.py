# coding: utf-8

"""
    Keap API

    Keap Public API Documentation

    The version of the OpenAPI document: v2
    Contact: api.keap@thryv.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class UpdateProductOptionRequest(BaseModel):
    """
    UpdateProductOptionRequest
    """ # noqa: E501
    required: Optional[StrictBool] = Field(default=None, description="Whether this option is required for product purchase.")
    option_label: Optional[StrictStr] = Field(default=None, description="The displayable name of the option (e.g. Size). Cannot be whitespace only.")
    display_order: Optional[StrictInt] = Field(default=None, description="The order in which this option will be displayed among other options. Minimum is 0. Lower values indicate higher priority in order.")
    minimum_characters: Optional[StrictInt] = Field(default=None, description="Used only for option_type of `TEXT`. Minimum allowable characters. Minimum is 0.")
    maximum_characters: Optional[StrictInt] = Field(default=None, description="Used only for option_type of `TEXT`. Maximum allowable characters. Minimum is 0.")
    allow_spaces: Optional[StrictBool] = Field(default=None, description="Used only for option_type of `TEXT`. Whether or not to allow whitespace in the text.")
    only_starts_with: Optional[StrictStr] = Field(default=None, description="Used only for option_type of `TEXT`.  Restricts the text to start with certain character types. Valid values are NONE, LETTER, DIGIT, BOTH.")
    only_ends_with: Optional[StrictStr] = Field(default=None, description="Used only for option_type of `TEXT`. Restricts the text to end with certain character types. Valid values are NONE, LETTER, DIGIT, BOTH.")
    only_contains: Optional[StrictStr] = Field(default=None, description="Used only for option_type of `TEXT`. Restricts the text to contain certain character types. Valid values are NONE, LETTER, DIGIT, BOTH.")
    error_message: Optional[StrictStr] = Field(default=None, description="Used only for option_type of `TEXT`. An error message to display if any rules are broken. Cannot be whitespace only.")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["required", "option_label", "display_order", "minimum_characters", "maximum_characters", "allow_spaces", "only_starts_with", "only_ends_with", "only_contains", "error_message"]

    @field_validator('only_starts_with')
    def only_starts_with_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NONE', 'LETTER', 'DIGIT', 'BOTH']):
            raise ValueError("must be one of enum values ('NONE', 'LETTER', 'DIGIT', 'BOTH')")
        return value

    @field_validator('only_ends_with')
    def only_ends_with_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NONE', 'LETTER', 'DIGIT', 'BOTH']):
            raise ValueError("must be one of enum values ('NONE', 'LETTER', 'DIGIT', 'BOTH')")
        return value

    @field_validator('only_contains')
    def only_contains_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['NONE', 'LETTER', 'DIGIT', 'BOTH']):
            raise ValueError("must be one of enum values ('NONE', 'LETTER', 'DIGIT', 'BOTH')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of UpdateProductOptionRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of UpdateProductOptionRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "required": obj.get("required"),
            "option_label": obj.get("option_label"),
            "display_order": obj.get("display_order"),
            "minimum_characters": obj.get("minimum_characters"),
            "maximum_characters": obj.get("maximum_characters"),
            "allow_spaces": obj.get("allow_spaces"),
            "only_starts_with": obj.get("only_starts_with"),
            "only_ends_with": obj.get("only_ends_with"),
            "only_contains": obj.get("only_contains"),
            "error_message": obj.get("error_message")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


