# coding: utf-8

"""
    Keap API

    Keap Public API Documentation

    The version of the OpenAPI document: v2
    Contact: api.keap@thryv.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class ProductVariableSettingList(BaseModel):
    """
    ProductVariableSettingList
    """ # noqa: E501
    contains: Optional[StrictStr] = Field(default=None, description="Requires the value (excluding start and end) to contain given type.")
    min_chars: Optional[StrictInt] = Field(default=None, description="Minimum allowable characters")
    max_chars: Optional[StrictInt] = Field(default=None, description="Max allowable chars. Its value must be greater than min_chars")
    contain_spaces: Optional[StrictBool] = Field(default=None, description="If spaces are allowed for the option")
    start_with: Optional[StrictStr] = Field(default=None, description="Requires the value to start with the given type.")
    end_with: Optional[StrictStr] = Field(default=None, description="Requires the value to end with the given type.")
    validation_msg: Optional[StrictStr] = Field(default=None, description="Customized validation message to display when option criteria aren't met")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["contains", "min_chars", "max_chars", "contain_spaces", "start_with", "end_with", "validation_msg"]

    @field_validator('contains')
    def contains_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['LETTER', 'NUMBER', 'BOTH']):
            raise ValueError("must be one of enum values ('LETTER', 'NUMBER', 'BOTH')")
        return value

    @field_validator('start_with')
    def start_with_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['LETTER', 'NUMBER', 'BOTH']):
            raise ValueError("must be one of enum values ('LETTER', 'NUMBER', 'BOTH')")
        return value

    @field_validator('end_with')
    def end_with_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['LETTER', 'NUMBER', 'BOTH']):
            raise ValueError("must be one of enum values ('LETTER', 'NUMBER', 'BOTH')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProductVariableSettingList from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProductVariableSettingList from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "contains": obj.get("contains"),
            "min_chars": obj.get("min_chars"),
            "max_chars": obj.get("max_chars"),
            "contain_spaces": obj.get("contain_spaces"),
            "start_with": obj.get("start_with"),
            "end_with": obj.get("end_with"),
            "validation_msg": obj.get("validation_msg")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


