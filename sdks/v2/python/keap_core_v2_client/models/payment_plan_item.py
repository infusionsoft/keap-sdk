# coding: utf-8

"""
    Keap API

    Keap Public API Documentation

    The version of the OpenAPI document: v2
    Contact: api.keap@thryv.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from keap_core_v2_client.models.currency_value import CurrencyValue
from typing import Optional, Set
from typing_extensions import Self

class PaymentPlanItem(BaseModel):
    """
    PaymentPlanItem
    """ # noqa: E501
    status: Optional[StrictStr] = Field(default=None, description="The status of the payment plan item. Possible values are: - `UNPAID_MANUAL` - The item is set for manual charge, and its `amount_due` has not been completely paid. - `UNPAID_AUTOCHARGE` - The item is set for auto-charge, and its `amount_due` has not been completely paid. - `PAID` - The item has been completely paid. - `FAILED` - The item `amount_due` has not been completely paid, and there was a failure. (*Note: This status is deprecated and not currently used.*) ")
    due_date: Optional[date] = Field(default=None, description="The due date in the application's timezone, as an ISO 8601 calendar date string (date only), in the format YYYY-MM-DD")
    amount_due: Optional[CurrencyValue] = None
    amount_paid: Optional[CurrencyValue] = None
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["status", "due_date", "amount_due", "amount_paid"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['UNPAID_MANUAL', 'UNPAID_AUTOCHARGE', 'PAID', 'FAILED']):
            raise ValueError("must be one of enum values ('UNPAID_MANUAL', 'UNPAID_AUTOCHARGE', 'PAID', 'FAILED')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PaymentPlanItem from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of amount_due
        if self.amount_due:
            _dict['amount_due'] = self.amount_due.to_dict()
        # override the default output from pydantic by calling `to_dict()` of amount_paid
        if self.amount_paid:
            _dict['amount_paid'] = self.amount_paid.to_dict()
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PaymentPlanItem from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status"),
            "due_date": obj.get("due_date"),
            "amount_due": CurrencyValue.from_dict(obj["amount_due"]) if obj.get("amount_due") is not None else None,
            "amount_paid": CurrencyValue.from_dict(obj["amount_paid"]) if obj.get("amount_paid") is not None else None
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


