/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CurrencyValue from './CurrencyValue';
import PaymentPlanItem from './PaymentPlanItem';

/**
 * The PaymentPlan model module.
 * @module keap.core.v2/model/PaymentPlan
 */
class PaymentPlan {
    /**
     * Constructs a new <code>PaymentPlan</code>.
     * @alias module:keap.core.v2/model/PaymentPlan
     * @param planStartDate {Date} The start date of the payment plan. In ISO-8601 date format (e.g. 2024-05-21)
     * @param numberOfPayments {Number} Number of payments in the plan
     * @param daysBetweenPayments {Number} Days between each payment
     */
    constructor(planStartDate, numberOfPayments, daysBetweenPayments) { 
        
        PaymentPlan.initialize(this, planStartDate, numberOfPayments, daysBetweenPayments);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, planStartDate, numberOfPayments, daysBetweenPayments) { 
        obj['plan_start_date'] = planStartDate;
        obj['number_of_payments'] = numberOfPayments;
        obj['days_between_payments'] = daysBetweenPayments;
    }

    /**
     * Constructs a <code>PaymentPlan</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/PaymentPlan} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/PaymentPlan} The populated <code>PaymentPlan</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentPlan();

            if (data.hasOwnProperty('auto_charge')) {
                obj['auto_charge'] = ApiClient.convertToType(data['auto_charge'], 'Boolean');
            }
            if (data.hasOwnProperty('max_charge_attempts')) {
                obj['max_charge_attempts'] = ApiClient.convertToType(data['max_charge_attempts'], 'Number');
            }
            if (data.hasOwnProperty('days_between_retries')) {
                obj['days_between_retries'] = ApiClient.convertToType(data['days_between_retries'], 'Number');
            }
            if (data.hasOwnProperty('payment_method_id')) {
                obj['payment_method_id'] = ApiClient.convertToType(data['payment_method_id'], 'String');
            }
            if (data.hasOwnProperty('initial_payment_amount')) {
                obj['initial_payment_amount'] = CurrencyValue.constructFromObject(data['initial_payment_amount']);
            }
            if (data.hasOwnProperty('initial_payment_percent')) {
                obj['initial_payment_percent'] = ApiClient.convertToType(data['initial_payment_percent'], 'Number');
            }
            if (data.hasOwnProperty('initial_payment_date')) {
                obj['initial_payment_date'] = ApiClient.convertToType(data['initial_payment_date'], 'Date');
            }
            if (data.hasOwnProperty('plan_start_date')) {
                obj['plan_start_date'] = ApiClient.convertToType(data['plan_start_date'], 'Date');
            }
            if (data.hasOwnProperty('number_of_payments')) {
                obj['number_of_payments'] = ApiClient.convertToType(data['number_of_payments'], 'Number');
            }
            if (data.hasOwnProperty('days_between_payments')) {
                obj['days_between_payments'] = ApiClient.convertToType(data['days_between_payments'], 'Number');
            }
            if (data.hasOwnProperty('payment_plan_items')) {
                obj['payment_plan_items'] = ApiClient.convertToType(data['payment_plan_items'], [PaymentPlanItem]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentPlan</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentPlan</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PaymentPlan.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['payment_method_id'] && !(typeof data['payment_method_id'] === 'string' || data['payment_method_id'] instanceof String)) {
            throw new Error("Expected the field `payment_method_id` to be a primitive type in the JSON string but got " + data['payment_method_id']);
        }
        // validate the optional field `initial_payment_amount`
        if (data['initial_payment_amount']) { // data not null
          CurrencyValue.validateJSON(data['initial_payment_amount']);
        }
        if (data['payment_plan_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['payment_plan_items'])) {
                throw new Error("Expected the field `payment_plan_items` to be an array in the JSON data but got " + data['payment_plan_items']);
            }
            // validate the optional field `payment_plan_items` (array)
            for (const item of data['payment_plan_items']) {
                PaymentPlanItem.validateJSON(item);
            };
        }

        return true;
    }


}

PaymentPlan.RequiredProperties = ["plan_start_date", "number_of_payments", "days_between_payments"];

/**
 * Whether to auto-charge payments
 * @member {Boolean} auto_charge
 */
PaymentPlan.prototype['auto_charge'] = undefined;

/**
 * Maximum charge attempts
 * @member {Number} max_charge_attempts
 */
PaymentPlan.prototype['max_charge_attempts'] = undefined;

/**
 * Days between retry attempts
 * @member {Number} days_between_retries
 */
PaymentPlan.prototype['days_between_retries'] = undefined;

/**
 * Payment method ID to charge
 * @member {String} payment_method_id
 */
PaymentPlan.prototype['payment_method_id'] = undefined;

/**
 * Initial payment amount
 * @member {module:keap.core.v2/model/CurrencyValue} initial_payment_amount
 */
PaymentPlan.prototype['initial_payment_amount'] = undefined;

/**
 * Initial payment as percentage
 * @member {Number} initial_payment_percent
 */
PaymentPlan.prototype['initial_payment_percent'] = undefined;

/**
 * The initial payment date. In ISO-8601 date format (e.g. 2024-05-21)
 * @member {Date} initial_payment_date
 */
PaymentPlan.prototype['initial_payment_date'] = undefined;

/**
 * The start date of the payment plan. In ISO-8601 date format (e.g. 2024-05-21)
 * @member {Date} plan_start_date
 */
PaymentPlan.prototype['plan_start_date'] = undefined;

/**
 * Number of payments in the plan
 * @member {Number} number_of_payments
 */
PaymentPlan.prototype['number_of_payments'] = undefined;

/**
 * Days between each payment
 * @member {Number} days_between_payments
 */
PaymentPlan.prototype['days_between_payments'] = undefined;

/**
 * List of scheduled payment items
 * @member {Array.<module:keap.core.v2/model/PaymentPlanItem>} payment_plan_items
 */
PaymentPlan.prototype['payment_plan_items'] = undefined;






export default PaymentPlan;
