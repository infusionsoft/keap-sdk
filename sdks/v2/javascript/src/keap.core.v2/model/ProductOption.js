/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ListOption from './ListOption';
import TextOption from './TextOption';

/**
 * The ProductOption model module.
 * @module keap.core.v2/model/ProductOption
 */
class ProductOption {
    /**
     * Constructs a new <code>ProductOption</code>.
     * @alias module:keap.core.v2/model/ProductOption
     */
    constructor() { 
        
        ProductOption.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ProductOption</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/ProductOption} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/ProductOption} The populated <code>ProductOption</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductOption();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('option_label')) {
                obj['option_label'] = ApiClient.convertToType(data['option_label'], 'String');
            }
            if (data.hasOwnProperty('product_id')) {
                obj['product_id'] = ApiClient.convertToType(data['product_id'], 'String');
            }
            if (data.hasOwnProperty('option_type')) {
                obj['option_type'] = ApiClient.convertToType(data['option_type'], 'String');
            }
            if (data.hasOwnProperty('display_order')) {
                obj['display_order'] = ApiClient.convertToType(data['display_order'], 'Number');
            }
            if (data.hasOwnProperty('list_items')) {
                obj['list_items'] = ApiClient.convertToType(data['list_items'], [ListOption]);
            }
            if (data.hasOwnProperty('text_rules')) {
                obj['text_rules'] = TextOption.constructFromObject(data['text_rules']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductOption</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductOption</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['option_label'] && !(typeof data['option_label'] === 'string' || data['option_label'] instanceof String)) {
            throw new Error("Expected the field `option_label` to be a primitive type in the JSON string but got " + data['option_label']);
        }
        // ensure the json data is a string
        if (data['product_id'] && !(typeof data['product_id'] === 'string' || data['product_id'] instanceof String)) {
            throw new Error("Expected the field `product_id` to be a primitive type in the JSON string but got " + data['product_id']);
        }
        // ensure the json data is a string
        if (data['option_type'] && !(typeof data['option_type'] === 'string' || data['option_type'] instanceof String)) {
            throw new Error("Expected the field `option_type` to be a primitive type in the JSON string but got " + data['option_type']);
        }
        if (data['list_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['list_items'])) {
                throw new Error("Expected the field `list_items` to be an array in the JSON data but got " + data['list_items']);
            }
            // validate the optional field `list_items` (array)
            for (const item of data['list_items']) {
                ListOption.validateJSON(item);
            };
        }
        // validate the optional field `text_rules`
        if (data['text_rules']) { // data not null
          TextOption.validateJSON(data['text_rules']);
        }

        return true;
    }


}



/**
 * The unique id of the product option
 * @member {String} id
 */
ProductOption.prototype['id'] = undefined;

/**
 * Whether this option is required for product purchase.
 * @member {Boolean} required
 */
ProductOption.prototype['required'] = undefined;

/**
 * The displayable name of the option. E.g. Size
 * @member {String} option_label
 */
ProductOption.prototype['option_label'] = undefined;

/**
 * The product id that this option belongs to.
 * @member {String} product_id
 */
ProductOption.prototype['product_id'] = undefined;

/**
 * The type of option. Possible valid values are LIST and TEXT.
 * @member {module:keap.core.v2/model/ProductOption.OptionTypeEnum} option_type
 */
ProductOption.prototype['option_type'] = undefined;

/**
 * The order in which this option will be displayed among other options. Minimum is 0. Lower values indicate higher priority in order.
 * @member {Number} display_order
 */
ProductOption.prototype['display_order'] = undefined;

/**
 * Appears only for option_type of LIST. A fixed list of available selectable items for this product option. e.g (Medium)
 * @member {Array.<module:keap.core.v2/model/ListOption>} list_items
 */
ProductOption.prototype['list_items'] = undefined;

/**
 * The option is a user-defined free-form field. The settings define the restrictions on what can be entered.
 * @member {module:keap.core.v2/model/TextOption} text_rules
 */
ProductOption.prototype['text_rules'] = undefined;





/**
 * Allowed values for the <code>option_type</code> property.
 * @enum {String}
 * @readonly
 */
ProductOption['OptionTypeEnum'] = {

    /**
     * value: "LIST"
     * @const
     */
    "LIST": "LIST",

    /**
     * value: "TEXT"
     * @const
     */
    "TEXT": "TEXT",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


export default ProductOption;
