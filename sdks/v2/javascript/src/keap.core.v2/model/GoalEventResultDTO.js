/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FlowEventResultDTO from './FlowEventResultDTO';

/**
 * The GoalEventResultDTO model module.
 * @module keap.core.v2/model/GoalEventResultDTO
 */
class GoalEventResultDTO {
    /**
     * Constructs a new <code>GoalEventResultDTO</code>.
     * Result of achieving a specific goal
     * @alias module:keap.core.v2/model/GoalEventResultDTO
     */
    constructor() { 
        
        GoalEventResultDTO.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>GoalEventResultDTO</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/GoalEventResultDTO} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/GoalEventResultDTO} The populated <code>GoalEventResultDTO</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GoalEventResultDTO();

            if (data.hasOwnProperty('success')) {
                obj['success'] = ApiClient.convertToType(data['success'], 'Boolean');
            }
            if (data.hasOwnProperty('message')) {
                obj['message'] = ApiClient.convertToType(data['message'], 'String');
            }
            if (data.hasOwnProperty('automation_id')) {
                obj['automation_id'] = ApiClient.convertToType(data['automation_id'], 'Number');
            }
            if (data.hasOwnProperty('goal_id')) {
                obj['goal_id'] = ApiClient.convertToType(data['goal_id'], 'Number');
            }
            if (data.hasOwnProperty('flow_start_results')) {
                obj['flow_start_results'] = ApiClient.convertToType(data['flow_start_results'], [FlowEventResultDTO]);
            }
            if (data.hasOwnProperty('flow_stop_results')) {
                obj['flow_stop_results'] = ApiClient.convertToType(data['flow_stop_results'], [FlowEventResultDTO]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GoalEventResultDTO</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GoalEventResultDTO</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['message'] && !(typeof data['message'] === 'string' || data['message'] instanceof String)) {
            throw new Error("Expected the field `message` to be a primitive type in the JSON string but got " + data['message']);
        }
        if (data['flow_start_results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['flow_start_results'])) {
                throw new Error("Expected the field `flow_start_results` to be an array in the JSON data but got " + data['flow_start_results']);
            }
            // validate the optional field `flow_start_results` (array)
            for (const item of data['flow_start_results']) {
                FlowEventResultDTO.validateJSON(item);
            };
        }
        if (data['flow_stop_results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['flow_stop_results'])) {
                throw new Error("Expected the field `flow_stop_results` to be an array in the JSON data but got " + data['flow_stop_results']);
            }
            // validate the optional field `flow_stop_results` (array)
            for (const item of data['flow_stop_results']) {
                FlowEventResultDTO.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Whether the goal was successfully achieved
 * @member {Boolean} success
 */
GoalEventResultDTO.prototype['success'] = undefined;

/**
 * A message with details about the result
 * @member {String} message
 */
GoalEventResultDTO.prototype['message'] = undefined;

/**
 * The automation ID where the goal was achieved
 * @member {Number} automation_id
 */
GoalEventResultDTO.prototype['automation_id'] = undefined;

/**
 * The goal ID that was achieved
 * @member {Number} goal_id
 */
GoalEventResultDTO.prototype['goal_id'] = undefined;

/**
 * List of flows that were started as a result of achieving this goal
 * @member {Array.<module:keap.core.v2/model/FlowEventResultDTO>} flow_start_results
 */
GoalEventResultDTO.prototype['flow_start_results'] = undefined;

/**
 * List of flows that were stopped as a result of achieving this goal
 * @member {Array.<module:keap.core.v2/model/FlowEventResultDTO>} flow_stop_results
 */
GoalEventResultDTO.prototype['flow_stop_results'] = undefined;






export default GoalEventResultDTO;
