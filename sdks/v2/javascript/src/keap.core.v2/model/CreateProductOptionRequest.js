/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CreateProductOptionListOption from './CreateProductOptionListOption';
import CreateProductOptionTextOption from './CreateProductOptionTextOption';

/**
 * The CreateProductOptionRequest model module.
 * @module keap.core.v2/model/CreateProductOptionRequest
 */
class CreateProductOptionRequest {
    /**
     * Constructs a new <code>CreateProductOptionRequest</code>.
     * @alias module:keap.core.v2/model/CreateProductOptionRequest
     * @param optionType {module:keap.core.v2/model/CreateProductOptionRequest.OptionTypeEnum} The type of option. Possible valid values are LIST and TEXT.
     */
    constructor(optionType) { 
        
        CreateProductOptionRequest.initialize(this, optionType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, optionType) { 
        obj['required'] = false;
        obj['option_type'] = optionType;
    }

    /**
     * Constructs a <code>CreateProductOptionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/CreateProductOptionRequest} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/CreateProductOptionRequest} The populated <code>CreateProductOptionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateProductOptionRequest();

            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('option_type')) {
                obj['option_type'] = ApiClient.convertToType(data['option_type'], 'String');
            }
            if (data.hasOwnProperty('option_label')) {
                obj['option_label'] = ApiClient.convertToType(data['option_label'], 'String');
            }
            if (data.hasOwnProperty('display_order')) {
                obj['display_order'] = ApiClient.convertToType(data['display_order'], 'Number');
            }
            if (data.hasOwnProperty('list_items')) {
                obj['list_items'] = ApiClient.convertToType(data['list_items'], [CreateProductOptionListOption]);
            }
            if (data.hasOwnProperty('text_rules')) {
                obj['text_rules'] = CreateProductOptionTextOption.constructFromObject(data['text_rules']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateProductOptionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateProductOptionRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateProductOptionRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['option_type'] && !(typeof data['option_type'] === 'string' || data['option_type'] instanceof String)) {
            throw new Error("Expected the field `option_type` to be a primitive type in the JSON string but got " + data['option_type']);
        }
        // ensure the json data is a string
        if (data['option_label'] && !(typeof data['option_label'] === 'string' || data['option_label'] instanceof String)) {
            throw new Error("Expected the field `option_label` to be a primitive type in the JSON string but got " + data['option_label']);
        }
        if (data['list_items']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['list_items'])) {
                throw new Error("Expected the field `list_items` to be an array in the JSON data but got " + data['list_items']);
            }
            // validate the optional field `list_items` (array)
            for (const item of data['list_items']) {
                CreateProductOptionListOption.validateJSON(item);
            };
        }
        // validate the optional field `text_rules`
        if (data['text_rules']) { // data not null
          CreateProductOptionTextOption.validateJSON(data['text_rules']);
        }

        return true;
    }


}

CreateProductOptionRequest.RequiredProperties = ["option_type"];

/**
 * Whether this option is required for product purchase.
 * @member {Boolean} required
 * @default false
 */
CreateProductOptionRequest.prototype['required'] = false;

/**
 * The type of option. Possible valid values are LIST and TEXT.
 * @member {module:keap.core.v2/model/CreateProductOptionRequest.OptionTypeEnum} option_type
 */
CreateProductOptionRequest.prototype['option_type'] = undefined;

/**
 * The displayable name of the option (e.g. Size). Cannot be whitespace only.
 * @member {String} option_label
 */
CreateProductOptionRequest.prototype['option_label'] = undefined;

/**
 * The order in which this option will be displayed among other options. Minimum is 0. Lower values indicate higher priority in order.
 * @member {Number} display_order
 */
CreateProductOptionRequest.prototype['display_order'] = undefined;

/**
 * Appears only for option_type of LIST. A fixed list of available selectable items for this product option.
 * @member {Array.<module:keap.core.v2/model/CreateProductOptionListOption>} list_items
 */
CreateProductOptionRequest.prototype['list_items'] = undefined;

/**
 * The option is a user-defined free-form field. The settings define the restrictions on what can be entered.
 * @member {module:keap.core.v2/model/CreateProductOptionTextOption} text_rules
 */
CreateProductOptionRequest.prototype['text_rules'] = undefined;





/**
 * Allowed values for the <code>option_type</code> property.
 * @enum {String}
 * @readonly
 */
CreateProductOptionRequest['OptionTypeEnum'] = {

    /**
     * value: "LIST"
     * @const
     */
    "LIST": "LIST",

    /**
     * value: "TEXT"
     * @const
     */
    "TEXT": "TEXT",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


export default CreateProductOptionRequest;
