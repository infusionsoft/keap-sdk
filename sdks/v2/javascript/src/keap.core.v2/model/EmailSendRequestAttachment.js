/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The EmailSendRequestAttachment model module.
 * @module keap.core.v2/model/EmailSendRequestAttachment
 */
class EmailSendRequestAttachment {
    /**
     * Constructs a new <code>EmailSendRequestAttachment</code>.
     * An attachment for an email
     * @alias module:keap.core.v2/model/EmailSendRequestAttachment
     * @param fileName {String} The filename of the attached file, including extension. The extension determines whether the attachment is accepted; only the following extensions are supported (case-insensitive): - Image: bmp, cdr, fpx, gif, jif, jiff, jp2, jpeg, jpg, jpx, j2k, pcd, pdf, png, psd, pspimage, thm, tif, tiff, yuv - Text/Document: doc, docx, err, log, msg, notes, pages, pwd, rtf, text, txt, vcf, wp, wp4, wp5, wp7, wpd, wps, wsd, xdl - Audio: mid, mp3, mp4, wav, wma - Video: avi, divx, f4p, flv, gvi, hdmov, hdv, m2ts, m4v, mnv, mov, movie, mp4, mp4v, mpeg, mpeg1, mpeg4, mpg, qt, rm, vob, wm, wmv - Data: csv, ics, key, pps, ppt, pptx, xlr, xls, xlsx, xml, zip  Requests including an attachment with any other extension return `400 Bad Request` with the message \"attachment type is invalid\". 
     * @param fileData {String} The content of the attachment, encoded in Base64
     */
    constructor(fileName, fileData) { 
        
        EmailSendRequestAttachment.initialize(this, fileName, fileData);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, fileName, fileData) { 
        obj['file_name'] = fileName;
        obj['file_data'] = fileData;
    }

    /**
     * Constructs a <code>EmailSendRequestAttachment</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/EmailSendRequestAttachment} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/EmailSendRequestAttachment} The populated <code>EmailSendRequestAttachment</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EmailSendRequestAttachment();

            if (data.hasOwnProperty('file_name')) {
                obj['file_name'] = ApiClient.convertToType(data['file_name'], 'String');
            }
            if (data.hasOwnProperty('file_data')) {
                obj['file_data'] = ApiClient.convertToType(data['file_data'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EmailSendRequestAttachment</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EmailSendRequestAttachment</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EmailSendRequestAttachment.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['file_name'] && !(typeof data['file_name'] === 'string' || data['file_name'] instanceof String)) {
            throw new Error("Expected the field `file_name` to be a primitive type in the JSON string but got " + data['file_name']);
        }
        // ensure the json data is a string
        if (data['file_data'] && !(typeof data['file_data'] === 'string' || data['file_data'] instanceof String)) {
            throw new Error("Expected the field `file_data` to be a primitive type in the JSON string but got " + data['file_data']);
        }

        return true;
    }


}

EmailSendRequestAttachment.RequiredProperties = ["file_name", "file_data"];

/**
 * The filename of the attached file, including extension. The extension determines whether the attachment is accepted; only the following extensions are supported (case-insensitive): - Image: bmp, cdr, fpx, gif, jif, jiff, jp2, jpeg, jpg, jpx, j2k, pcd, pdf, png, psd, pspimage, thm, tif, tiff, yuv - Text/Document: doc, docx, err, log, msg, notes, pages, pwd, rtf, text, txt, vcf, wp, wp4, wp5, wp7, wpd, wps, wsd, xdl - Audio: mid, mp3, mp4, wav, wma - Video: avi, divx, f4p, flv, gvi, hdmov, hdv, m2ts, m4v, mnv, mov, movie, mp4, mp4v, mpeg, mpeg1, mpeg4, mpg, qt, rm, vob, wm, wmv - Data: csv, ics, key, pps, ppt, pptx, xlr, xls, xlsx, xml, zip  Requests including an attachment with any other extension return `400 Bad Request` with the message \"attachment type is invalid\". 
 * @member {String} file_name
 */
EmailSendRequestAttachment.prototype['file_name'] = undefined;

/**
 * The content of the attachment, encoded in Base64
 * @member {String} file_data
 */
EmailSendRequestAttachment.prototype['file_data'] = undefined;






export default EmailSendRequestAttachment;
