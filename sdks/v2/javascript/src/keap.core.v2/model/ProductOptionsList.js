/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProductFixedOptionList from './ProductFixedOptionList';
import ProductVariableSettingList from './ProductVariableSettingList';

/**
 * The ProductOptionsList model module.
 * @module keap.core.v2/model/ProductOptionsList
 */
class ProductOptionsList {
    /**
     * Constructs a new <code>ProductOptionsList</code>.
     * @alias module:keap.core.v2/model/ProductOptionsList
     */
    constructor() { 
        
        ProductOptionsList.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>ProductOptionsList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/ProductOptionsList} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/ProductOptionsList} The populated <code>ProductOptionsList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProductOptionsList();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('fixed_options')) {
                obj['fixed_options'] = ApiClient.convertToType(data['fixed_options'], [ProductFixedOptionList]);
            }
            if (data.hasOwnProperty('variable_setting')) {
                obj['variable_setting'] = ProductVariableSettingList.constructFromObject(data['variable_setting']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProductOptionsList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProductOptionsList</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }
        if (data['fixed_options']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['fixed_options'])) {
                throw new Error("Expected the field `fixed_options` to be an array in the JSON data but got " + data['fixed_options']);
            }
            // validate the optional field `fixed_options` (array)
            for (const item of data['fixed_options']) {
                ProductFixedOptionList.validateJSON(item);
            };
        }
        // validate the optional field `variable_setting`
        if (data['variable_setting']) { // data not null
          ProductVariableSettingList.validateJSON(data['variable_setting']);
        }

        return true;
    }


}



/**
 * The option name
 * @member {String} name
 */
ProductOptionsList.prototype['name'] = undefined;

/**
 * The option type. If FIXED, then fixed_options will be returned. If VARIABLE, then variable_setting will be returned.
 * @member {module:keap.core.v2/model/ProductOptionsList.TypeEnum} type
 */
ProductOptionsList.prototype['type'] = undefined;

/**
 * If the user is required to select/fill in an option for the product
 * @member {Boolean} required
 */
ProductOptionsList.prototype['required'] = undefined;

/**
 * List of option values for the fixed option
 * @member {Array.<module:keap.core.v2/model/ProductFixedOptionList>} fixed_options
 */
ProductOptionsList.prototype['fixed_options'] = undefined;

/**
 * The option is a user-defined free-form field. The settings define the restrictions on what can be entered.
 * @member {module:keap.core.v2/model/ProductVariableSettingList} variable_setting
 */
ProductOptionsList.prototype['variable_setting'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
ProductOptionsList['TypeEnum'] = {

    /**
     * value: "FIXED"
     * @const
     */
    "FIXED": "FIXED",

    /**
     * value: "VARIABLE"
     * @const
     */
    "VARIABLE": "VARIABLE",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


export default ProductOptionsList;
