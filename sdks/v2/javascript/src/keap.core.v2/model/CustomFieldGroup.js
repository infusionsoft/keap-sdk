/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CustomFieldGroup model module.
 * @module keap.core.v2/model/CustomFieldGroup
 */
class CustomFieldGroup {
    /**
     * Constructs a new <code>CustomFieldGroup</code>.
     * @alias module:keap.core.v2/model/CustomFieldGroup
     */
    constructor() { 
        
        CustomFieldGroup.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CustomFieldGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/CustomFieldGroup} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/CustomFieldGroup} The populated <code>CustomFieldGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomFieldGroup();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('order')) {
                obj['order'] = ApiClient.convertToType(data['order'], 'Number');
            }
            if (data.hasOwnProperty('tab_id')) {
                obj['tab_id'] = ApiClient.convertToType(data['tab_id'], 'String');
            }
            if (data.hasOwnProperty('record_type')) {
                obj['record_type'] = ApiClient.convertToType(data['record_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomFieldGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomFieldGroup</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['tab_id'] && !(typeof data['tab_id'] === 'string' || data['tab_id'] instanceof String)) {
            throw new Error("Expected the field `tab_id` to be a primitive type in the JSON string but got " + data['tab_id']);
        }
        // ensure the json data is a string
        if (data['record_type'] && !(typeof data['record_type'] === 'string' || data['record_type'] instanceof String)) {
            throw new Error("Expected the field `record_type` to be a primitive type in the JSON string but got " + data['record_type']);
        }

        return true;
    }


}



/**
 * @member {String} id
 */
CustomFieldGroup.prototype['id'] = undefined;

/**
 * @member {String} name
 */
CustomFieldGroup.prototype['name'] = undefined;

/**
 * @member {Number} order
 */
CustomFieldGroup.prototype['order'] = undefined;

/**
 * @member {String} tab_id
 */
CustomFieldGroup.prototype['tab_id'] = undefined;

/**
 * @member {module:keap.core.v2/model/CustomFieldGroup.RecordTypeEnum} record_type
 */
CustomFieldGroup.prototype['record_type'] = undefined;





/**
 * Allowed values for the <code>record_type</code> property.
 * @enum {String}
 * @readonly
 */
CustomFieldGroup['RecordTypeEnum'] = {

    /**
     * value: "CONTACT"
     * @const
     */
    "CONTACT": "CONTACT",

    /**
     * value: "REFERRAL_PARTNER"
     * @const
     */
    "REFERRAL_PARTNER": "REFERRAL_PARTNER",

    /**
     * value: "OPPORTUNITY"
     * @const
     */
    "OPPORTUNITY": "OPPORTUNITY",

    /**
     * value: "TASK_NOTE_APPOINTMENT"
     * @const
     */
    "TASK_NOTE_APPOINTMENT": "TASK_NOTE_APPOINTMENT",

    /**
     * value: "COMPANY"
     * @const
     */
    "COMPANY": "COMPANY",

    /**
     * value: "ORDER"
     * @const
     */
    "ORDER": "ORDER",

    /**
     * value: "SUBSCRIPTION"
     * @const
     */
    "SUBSCRIPTION": "SUBSCRIPTION",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


export default CustomFieldGroup;
