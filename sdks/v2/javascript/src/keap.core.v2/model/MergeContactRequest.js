/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The MergeContactRequest model module.
 * @module keap.core.v2/model/MergeContactRequest
 */
class MergeContactRequest {
    /**
     * Constructs a new <code>MergeContactRequest</code>.
     * @alias module:keap.core.v2/model/MergeContactRequest
     * @param contactId {String} Primary contact ID to merge into
     * @param duplicateContactId {String} Duplicate contact ID to merge from
     */
    constructor(contactId, duplicateContactId) { 
        
        MergeContactRequest.initialize(this, contactId, duplicateContactId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contactId, duplicateContactId) { 
        obj['contact_id'] = contactId;
        obj['duplicate_contact_id'] = duplicateContactId;
    }

    /**
     * Constructs a <code>MergeContactRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/MergeContactRequest} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/MergeContactRequest} The populated <code>MergeContactRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new MergeContactRequest();

            if (data.hasOwnProperty('contact_id')) {
                obj['contact_id'] = ApiClient.convertToType(data['contact_id'], 'String');
            }
            if (data.hasOwnProperty('duplicate_contact_id')) {
                obj['duplicate_contact_id'] = ApiClient.convertToType(data['duplicate_contact_id'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>MergeContactRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>MergeContactRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of MergeContactRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['contact_id'] && !(typeof data['contact_id'] === 'string' || data['contact_id'] instanceof String)) {
            throw new Error("Expected the field `contact_id` to be a primitive type in the JSON string but got " + data['contact_id']);
        }
        // ensure the json data is a string
        if (data['duplicate_contact_id'] && !(typeof data['duplicate_contact_id'] === 'string' || data['duplicate_contact_id'] instanceof String)) {
            throw new Error("Expected the field `duplicate_contact_id` to be a primitive type in the JSON string but got " + data['duplicate_contact_id']);
        }

        return true;
    }


}

MergeContactRequest.RequiredProperties = ["contact_id", "duplicate_contact_id"];

/**
 * Primary contact ID to merge into
 * @member {String} contact_id
 */
MergeContactRequest.prototype['contact_id'] = undefined;

/**
 * Duplicate contact ID to merge from
 * @member {String} duplicate_contact_id
 */
MergeContactRequest.prototype['duplicate_contact_id'] = undefined;






export default MergeContactRequest;
