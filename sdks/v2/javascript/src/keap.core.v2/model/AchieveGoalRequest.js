/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AchieveGoalRequest model module.
 * @module keap.core.v2/model/AchieveGoalRequest
 */
class AchieveGoalRequest {
    /**
     * Constructs a new <code>AchieveGoalRequest</code>.
     * Request to achieve a goal in an automation. You must provide EITHER (integration + call_name) OR (automation_id + goal_id), along with contact_id.
     * @alias module:keap.core.v2/model/AchieveGoalRequest
     * @param contactId {Number} The contact ID for whom to achieve the goal
     */
    constructor(contactId) { 
        
        AchieveGoalRequest.initialize(this, contactId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, contactId) { 
        obj['contact_id'] = contactId;
    }

    /**
     * Constructs a <code>AchieveGoalRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/AchieveGoalRequest} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/AchieveGoalRequest} The populated <code>AchieveGoalRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AchieveGoalRequest();

            if (data.hasOwnProperty('integration')) {
                obj['integration'] = ApiClient.convertToType(data['integration'], 'String');
            }
            if (data.hasOwnProperty('call_name')) {
                obj['call_name'] = ApiClient.convertToType(data['call_name'], 'String');
            }
            if (data.hasOwnProperty('automation_id')) {
                obj['automation_id'] = ApiClient.convertToType(data['automation_id'], 'Number');
            }
            if (data.hasOwnProperty('goal_id')) {
                obj['goal_id'] = ApiClient.convertToType(data['goal_id'], 'Number');
            }
            if (data.hasOwnProperty('contact_id')) {
                obj['contact_id'] = ApiClient.convertToType(data['contact_id'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AchieveGoalRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AchieveGoalRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AchieveGoalRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['integration'] && !(typeof data['integration'] === 'string' || data['integration'] instanceof String)) {
            throw new Error("Expected the field `integration` to be a primitive type in the JSON string but got " + data['integration']);
        }
        // ensure the json data is a string
        if (data['call_name'] && !(typeof data['call_name'] === 'string' || data['call_name'] instanceof String)) {
            throw new Error("Expected the field `call_name` to be a primitive type in the JSON string but got " + data['call_name']);
        }

        return true;
    }


}

AchieveGoalRequest.RequiredProperties = ["contact_id"];

/**
 * The integration name of the goal. Required when using integration/call_name approach. Use together with 'call_name'. Mutually exclusive with automation_id/goal_id.
 * @member {String} integration
 */
AchieveGoalRequest.prototype['integration'] = undefined;

/**
 * The call name of the goal. Required when using integration/call_name approach. Use together with 'integration'. Mutually exclusive with automation_id/goal_id.
 * @member {String} call_name
 */
AchieveGoalRequest.prototype['call_name'] = undefined;

/**
 * The automation ID. Required when using automation_id/goal_id approach. Use together with 'goal_id'. Mutually exclusive with integration/call_name.
 * @member {Number} automation_id
 */
AchieveGoalRequest.prototype['automation_id'] = undefined;

/**
 * The goal ID within the automation. Required when using automation_id/goal_id approach. Use together with 'automation_id'. Mutually exclusive with integration/call_name.
 * @member {Number} goal_id
 */
AchieveGoalRequest.prototype['goal_id'] = undefined;

/**
 * The contact ID for whom to achieve the goal
 * @member {Number} contact_id
 */
AchieveGoalRequest.prototype['contact_id'] = undefined;






export default AchieveGoalRequest;
