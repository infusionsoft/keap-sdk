/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UpdateProductOptionRequest model module.
 * @module keap.core.v2/model/UpdateProductOptionRequest
 */
class UpdateProductOptionRequest {
    /**
     * Constructs a new <code>UpdateProductOptionRequest</code>.
     * @alias module:keap.core.v2/model/UpdateProductOptionRequest
     */
    constructor() { 
        
        UpdateProductOptionRequest.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>UpdateProductOptionRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/UpdateProductOptionRequest} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/UpdateProductOptionRequest} The populated <code>UpdateProductOptionRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateProductOptionRequest();

            if (data.hasOwnProperty('required')) {
                obj['required'] = ApiClient.convertToType(data['required'], 'Boolean');
            }
            if (data.hasOwnProperty('option_label')) {
                obj['option_label'] = ApiClient.convertToType(data['option_label'], 'String');
            }
            if (data.hasOwnProperty('display_order')) {
                obj['display_order'] = ApiClient.convertToType(data['display_order'], 'Number');
            }
            if (data.hasOwnProperty('minimum_characters')) {
                obj['minimum_characters'] = ApiClient.convertToType(data['minimum_characters'], 'Number');
            }
            if (data.hasOwnProperty('maximum_characters')) {
                obj['maximum_characters'] = ApiClient.convertToType(data['maximum_characters'], 'Number');
            }
            if (data.hasOwnProperty('allow_spaces')) {
                obj['allow_spaces'] = ApiClient.convertToType(data['allow_spaces'], 'Boolean');
            }
            if (data.hasOwnProperty('only_starts_with')) {
                obj['only_starts_with'] = ApiClient.convertToType(data['only_starts_with'], 'String');
            }
            if (data.hasOwnProperty('only_ends_with')) {
                obj['only_ends_with'] = ApiClient.convertToType(data['only_ends_with'], 'String');
            }
            if (data.hasOwnProperty('only_contains')) {
                obj['only_contains'] = ApiClient.convertToType(data['only_contains'], 'String');
            }
            if (data.hasOwnProperty('error_message')) {
                obj['error_message'] = ApiClient.convertToType(data['error_message'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateProductOptionRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateProductOptionRequest</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['option_label'] && !(typeof data['option_label'] === 'string' || data['option_label'] instanceof String)) {
            throw new Error("Expected the field `option_label` to be a primitive type in the JSON string but got " + data['option_label']);
        }
        // ensure the json data is a string
        if (data['only_starts_with'] && !(typeof data['only_starts_with'] === 'string' || data['only_starts_with'] instanceof String)) {
            throw new Error("Expected the field `only_starts_with` to be a primitive type in the JSON string but got " + data['only_starts_with']);
        }
        // ensure the json data is a string
        if (data['only_ends_with'] && !(typeof data['only_ends_with'] === 'string' || data['only_ends_with'] instanceof String)) {
            throw new Error("Expected the field `only_ends_with` to be a primitive type in the JSON string but got " + data['only_ends_with']);
        }
        // ensure the json data is a string
        if (data['only_contains'] && !(typeof data['only_contains'] === 'string' || data['only_contains'] instanceof String)) {
            throw new Error("Expected the field `only_contains` to be a primitive type in the JSON string but got " + data['only_contains']);
        }
        // ensure the json data is a string
        if (data['error_message'] && !(typeof data['error_message'] === 'string' || data['error_message'] instanceof String)) {
            throw new Error("Expected the field `error_message` to be a primitive type in the JSON string but got " + data['error_message']);
        }

        return true;
    }


}



/**
 * Whether this option is required for product purchase.
 * @member {Boolean} required
 */
UpdateProductOptionRequest.prototype['required'] = undefined;

/**
 * The displayable name of the option (e.g. Size). Cannot be whitespace only.
 * @member {String} option_label
 */
UpdateProductOptionRequest.prototype['option_label'] = undefined;

/**
 * The order in which this option will be displayed among other options. Minimum is 0. Lower values indicate higher priority in order.
 * @member {Number} display_order
 */
UpdateProductOptionRequest.prototype['display_order'] = undefined;

/**
 * Used only for option_type of `TEXT`. Minimum allowable characters. Minimum is 0.
 * @member {Number} minimum_characters
 */
UpdateProductOptionRequest.prototype['minimum_characters'] = undefined;

/**
 * Used only for option_type of `TEXT`. Maximum allowable characters. Minimum is 0.
 * @member {Number} maximum_characters
 */
UpdateProductOptionRequest.prototype['maximum_characters'] = undefined;

/**
 * Used only for option_type of `TEXT`. Whether or not to allow whitespace in the text.
 * @member {Boolean} allow_spaces
 */
UpdateProductOptionRequest.prototype['allow_spaces'] = undefined;

/**
 * Used only for option_type of `TEXT`.  Restricts the text to start with certain character types. Valid values are NONE, LETTER, DIGIT, BOTH.
 * @member {module:keap.core.v2/model/UpdateProductOptionRequest.OnlyStartsWithEnum} only_starts_with
 */
UpdateProductOptionRequest.prototype['only_starts_with'] = undefined;

/**
 * Used only for option_type of `TEXT`. Restricts the text to end with certain character types. Valid values are NONE, LETTER, DIGIT, BOTH.
 * @member {module:keap.core.v2/model/UpdateProductOptionRequest.OnlyEndsWithEnum} only_ends_with
 */
UpdateProductOptionRequest.prototype['only_ends_with'] = undefined;

/**
 * Used only for option_type of `TEXT`. Restricts the text to contain certain character types. Valid values are NONE, LETTER, DIGIT, BOTH.
 * @member {module:keap.core.v2/model/UpdateProductOptionRequest.OnlyContainsEnum} only_contains
 */
UpdateProductOptionRequest.prototype['only_contains'] = undefined;

/**
 * Used only for option_type of `TEXT`. An error message to display if any rules are broken. Cannot be whitespace only.
 * @member {String} error_message
 */
UpdateProductOptionRequest.prototype['error_message'] = undefined;





/**
 * Allowed values for the <code>only_starts_with</code> property.
 * @enum {String}
 * @readonly
 */
UpdateProductOptionRequest['OnlyStartsWithEnum'] = {

    /**
     * value: "NONE"
     * @const
     */
    "NONE": "NONE",

    /**
     * value: "LETTER"
     * @const
     */
    "LETTER": "LETTER",

    /**
     * value: "DIGIT"
     * @const
     */
    "DIGIT": "DIGIT",

    /**
     * value: "BOTH"
     * @const
     */
    "BOTH": "BOTH",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};

/**
 * Allowed values for the <code>only_ends_with</code> property.
 * @enum {String}
 * @readonly
 */
UpdateProductOptionRequest['OnlyEndsWithEnum'] = {

    /**
     * value: "NONE"
     * @const
     */
    "NONE": "NONE",

    /**
     * value: "LETTER"
     * @const
     */
    "LETTER": "LETTER",

    /**
     * value: "DIGIT"
     * @const
     */
    "DIGIT": "DIGIT",

    /**
     * value: "BOTH"
     * @const
     */
    "BOTH": "BOTH",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};

/**
 * Allowed values for the <code>only_contains</code> property.
 * @enum {String}
 * @readonly
 */
UpdateProductOptionRequest['OnlyContainsEnum'] = {

    /**
     * value: "NONE"
     * @const
     */
    "NONE": "NONE",

    /**
     * value: "LETTER"
     * @const
     */
    "LETTER": "LETTER",

    /**
     * value: "DIGIT"
     * @const
     */
    "DIGIT": "DIGIT",

    /**
     * value: "BOTH"
     * @const
     */
    "BOTH": "BOTH",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


export default UpdateProductOptionRequest;
