/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CurrencyValue from './CurrencyValue';

/**
 * The PaymentPlanItem model module.
 * @module keap.core.v2/model/PaymentPlanItem
 */
class PaymentPlanItem {
    /**
     * Constructs a new <code>PaymentPlanItem</code>.
     * @alias module:keap.core.v2/model/PaymentPlanItem
     */
    constructor() { 
        
        PaymentPlanItem.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PaymentPlanItem</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/PaymentPlanItem} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/PaymentPlanItem} The populated <code>PaymentPlanItem</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaymentPlanItem();

            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('due_date')) {
                obj['due_date'] = ApiClient.convertToType(data['due_date'], 'Date');
            }
            if (data.hasOwnProperty('amount_due')) {
                obj['amount_due'] = CurrencyValue.constructFromObject(data['amount_due']);
            }
            if (data.hasOwnProperty('amount_paid')) {
                obj['amount_paid'] = CurrencyValue.constructFromObject(data['amount_paid']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaymentPlanItem</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaymentPlanItem</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // validate the optional field `amount_due`
        if (data['amount_due']) { // data not null
          CurrencyValue.validateJSON(data['amount_due']);
        }
        // validate the optional field `amount_paid`
        if (data['amount_paid']) { // data not null
          CurrencyValue.validateJSON(data['amount_paid']);
        }

        return true;
    }


}



/**
 * The status of the payment plan item. Possible values are: - `UNPAID_MANUAL` - The item is set for manual charge, and its `amount_due` has not been completely paid. - `UNPAID_AUTOCHARGE` - The item is set for auto-charge, and its `amount_due` has not been completely paid. - `PAID` - The item has been completely paid. - `FAILED` - The item `amount_due` has not been completely paid, and there was a failure. (*Note: This status is deprecated and not currently used.*) 
 * @member {module:keap.core.v2/model/PaymentPlanItem.StatusEnum} status
 */
PaymentPlanItem.prototype['status'] = undefined;

/**
 * The due date in the application's timezone, as an ISO 8601 calendar date string (date only), in the format YYYY-MM-DD
 * @member {Date} due_date
 */
PaymentPlanItem.prototype['due_date'] = undefined;

/**
 * @member {module:keap.core.v2/model/CurrencyValue} amount_due
 */
PaymentPlanItem.prototype['amount_due'] = undefined;

/**
 * @member {module:keap.core.v2/model/CurrencyValue} amount_paid
 */
PaymentPlanItem.prototype['amount_paid'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
PaymentPlanItem['StatusEnum'] = {

    /**
     * value: "UNPAID_MANUAL"
     * @const
     */
    "UNPAID_MANUAL": "UNPAID_MANUAL",

    /**
     * value: "UNPAID_AUTOCHARGE"
     * @const
     */
    "UNPAID_AUTOCHARGE": "UNPAID_AUTOCHARGE",

    /**
     * value: "PAID"
     * @const
     */
    "PAID": "PAID",

    /**
     * value: "FAILED"
     * @const
     */
    "FAILED": "FAILED",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


export default PaymentPlanItem;
