/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The OpportunityStageMove model module.
 * @module keap.core.v2/model/OpportunityStageMove
 */
class OpportunityStageMove {
    /**
     * Constructs a new <code>OpportunityStageMove</code>.
     * Historical record of an opportunity moving from one pipeline stage to another
     * @alias module:keap.core.v2/model/OpportunityStageMove
     */
    constructor() { 
        
        OpportunityStageMove.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>OpportunityStageMove</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/OpportunityStageMove} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/OpportunityStageMove} The populated <code>OpportunityStageMove</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new OpportunityStageMove();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('opportunity_id')) {
                obj['opportunity_id'] = ApiClient.convertToType(data['opportunity_id'], 'String');
            }
            if (data.hasOwnProperty('move_time')) {
                obj['move_time'] = ApiClient.convertToType(data['move_time'], 'String');
            }
            if (data.hasOwnProperty('move_from_stage_id')) {
                obj['move_from_stage_id'] = ApiClient.convertToType(data['move_from_stage_id'], 'String');
            }
            if (data.hasOwnProperty('move_to_stage_id')) {
                obj['move_to_stage_id'] = ApiClient.convertToType(data['move_to_stage_id'], 'String');
            }
            if (data.hasOwnProperty('prev_stage_move_time')) {
                obj['prev_stage_move_time'] = ApiClient.convertToType(data['prev_stage_move_time'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('created_by')) {
                obj['created_by'] = ApiClient.convertToType(data['created_by'], 'String');
            }
            if (data.hasOwnProperty('create_time')) {
                obj['create_time'] = ApiClient.convertToType(data['create_time'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>OpportunityStageMove</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>OpportunityStageMove</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['opportunity_id'] && !(typeof data['opportunity_id'] === 'string' || data['opportunity_id'] instanceof String)) {
            throw new Error("Expected the field `opportunity_id` to be a primitive type in the JSON string but got " + data['opportunity_id']);
        }
        // ensure the json data is a string
        if (data['move_time'] && !(typeof data['move_time'] === 'string' || data['move_time'] instanceof String)) {
            throw new Error("Expected the field `move_time` to be a primitive type in the JSON string but got " + data['move_time']);
        }
        // ensure the json data is a string
        if (data['move_from_stage_id'] && !(typeof data['move_from_stage_id'] === 'string' || data['move_from_stage_id'] instanceof String)) {
            throw new Error("Expected the field `move_from_stage_id` to be a primitive type in the JSON string but got " + data['move_from_stage_id']);
        }
        // ensure the json data is a string
        if (data['move_to_stage_id'] && !(typeof data['move_to_stage_id'] === 'string' || data['move_to_stage_id'] instanceof String)) {
            throw new Error("Expected the field `move_to_stage_id` to be a primitive type in the JSON string but got " + data['move_to_stage_id']);
        }
        // ensure the json data is a string
        if (data['prev_stage_move_time'] && !(typeof data['prev_stage_move_time'] === 'string' || data['prev_stage_move_time'] instanceof String)) {
            throw new Error("Expected the field `prev_stage_move_time` to be a primitive type in the JSON string but got " + data['prev_stage_move_time']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['created_by'] && !(typeof data['created_by'] === 'string' || data['created_by'] instanceof String)) {
            throw new Error("Expected the field `created_by` to be a primitive type in the JSON string but got " + data['created_by']);
        }
        // ensure the json data is a string
        if (data['create_time'] && !(typeof data['create_time'] === 'string' || data['create_time'] instanceof String)) {
            throw new Error("Expected the field `create_time` to be a primitive type in the JSON string but got " + data['create_time']);
        }

        return true;
    }


}



/**
 * Unique ID of this stage-move record
 * @member {String} id
 */
OpportunityStageMove.prototype['id'] = undefined;

/**
 * ID of the opportunity that was moved
 * @member {String} opportunity_id
 */
OpportunityStageMove.prototype['opportunity_id'] = undefined;

/**
 * Date-time when the move occurred (ISO-8601 UTC)
 * @member {String} move_time
 */
OpportunityStageMove.prototype['move_time'] = undefined;

/**
 * ID of the stage moved FROM. 0 = no previous stage (first move)
 * @member {String} move_from_stage_id
 */
OpportunityStageMove.prototype['move_from_stage_id'] = undefined;

/**
 * ID of the stage moved TO
 * @member {String} move_to_stage_id
 */
OpportunityStageMove.prototype['move_to_stage_id'] = undefined;

/**
 * Date-time of the previous stage-move (ISO-8601 UTC). Null if first move.
 * @member {String} prev_stage_move_time
 */
OpportunityStageMove.prototype['prev_stage_move_time'] = undefined;

/**
 * ID of the user associated with this move
 * @member {String} user_id
 */
OpportunityStageMove.prototype['user_id'] = undefined;

/**
 * ID of the user who created this record
 * @member {String} created_by
 */
OpportunityStageMove.prototype['created_by'] = undefined;

/**
 * Date-time this record was created (ISO-8601 UTC)
 * @member {String} create_time
 */
OpportunityStageMove.prototype['create_time'] = undefined;






export default OpportunityStageMove;
