/**
 * Keap REST API
 * Keap REST Core Service
 *
 * The version of the OpenAPI document: 2.70.0.809589
 * Contact: api@keap.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DisplayProperty from './DisplayProperty';

/**
 * The DisplayGroup model module.
 * @module keap.core.v2/model/DisplayGroup
 */
class DisplayGroup {
    /**
     * Constructs a new <code>DisplayGroup</code>.
     * A group of properties on a display form
     * @alias module:keap.core.v2/model/DisplayGroup
     */
    constructor() { 
        
        DisplayGroup.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DisplayGroup</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/DisplayGroup} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/DisplayGroup} The populated <code>DisplayGroup</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DisplayGroup();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('sort_order')) {
                obj['sort_order'] = ApiClient.convertToType(data['sort_order'], 'Number');
            }
            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('properties')) {
                obj['properties'] = ApiClient.convertToType(data['properties'], [DisplayProperty]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DisplayGroup</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DisplayGroup</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        if (data['properties']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['properties'])) {
                throw new Error("Expected the field `properties` to be an array in the JSON data but got " + data['properties']);
            }
            // validate the optional field `properties` (array)
            for (const item of data['properties']) {
                DisplayProperty.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Unique identifier for the display group.
 * @member {String} id
 */
DisplayGroup.prototype['id'] = undefined;

/**
 * Determines the order for where this display group shows up on the form.
 * @member {Number} sort_order
 */
DisplayGroup.prototype['sort_order'] = undefined;

/**
 * The title of the display group.
 * @member {String} title
 */
DisplayGroup.prototype['title'] = undefined;

/**
 * The description of the display group.
 * @member {String} description
 */
DisplayGroup.prototype['description'] = undefined;

/**
 * A list of properties that will be displayed in this group.
 * @member {Array.<module:keap.core.v2/model/DisplayProperty>} properties
 */
DisplayGroup.prototype['properties'] = undefined;






export default DisplayGroup;
