/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CreateLeadSourceRecurringExpenseRequest model module.
 * @module keap.core.v2/model/CreateLeadSourceRecurringExpenseRequest
 */
class CreateLeadSourceRecurringExpenseRequest {
    /**
     * Constructs a new <code>CreateLeadSourceRecurringExpenseRequest</code>.
     * @alias module:keap.core.v2/model/CreateLeadSourceRecurringExpenseRequest
     * @param amount {Number} The monthly cost of the lead source recurring expense. The value should be in the smallest unit of currency for your currency locale. For example, if your currency locale is USD, then the smallest unit of currency is in cents, $225.50 would be provided in the request as 22550.
     */
    constructor(amount) { 
        
        CreateLeadSourceRecurringExpenseRequest.initialize(this, amount);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, amount) { 
        obj['amount'] = amount;
    }

    /**
     * Constructs a <code>CreateLeadSourceRecurringExpenseRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/CreateLeadSourceRecurringExpenseRequest} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/CreateLeadSourceRecurringExpenseRequest} The populated <code>CreateLeadSourceRecurringExpenseRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateLeadSourceRecurringExpenseRequest();

            if (data.hasOwnProperty('title')) {
                obj['title'] = ApiClient.convertToType(data['title'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('amount')) {
                obj['amount'] = ApiClient.convertToType(data['amount'], 'Number');
            }
            if (data.hasOwnProperty('start_time')) {
                obj['start_time'] = ApiClient.convertToType(data['start_time'], 'Date');
            }
            if (data.hasOwnProperty('end_time')) {
                obj['end_time'] = ApiClient.convertToType(data['end_time'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateLeadSourceRecurringExpenseRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateLeadSourceRecurringExpenseRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateLeadSourceRecurringExpenseRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['title'] && !(typeof data['title'] === 'string' || data['title'] instanceof String)) {
            throw new Error("Expected the field `title` to be a primitive type in the JSON string but got " + data['title']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }

        return true;
    }


}

CreateLeadSourceRecurringExpenseRequest.RequiredProperties = ["amount"];

/**
 * The title of the lead source recurring expense
 * @member {String} title
 */
CreateLeadSourceRecurringExpenseRequest.prototype['title'] = undefined;

/**
 * The notes for the lead source recurring expense
 * @member {String} notes
 */
CreateLeadSourceRecurringExpenseRequest.prototype['notes'] = undefined;

/**
 * The monthly cost of the lead source recurring expense. The value should be in the smallest unit of currency for your currency locale. For example, if your currency locale is USD, then the smallest unit of currency is in cents, $225.50 would be provided in the request as 22550.
 * @member {Number} amount
 */
CreateLeadSourceRecurringExpenseRequest.prototype['amount'] = undefined;

/**
 * The time the lead source recurring expense starts
 * @member {Date} start_time
 */
CreateLeadSourceRecurringExpenseRequest.prototype['start_time'] = undefined;

/**
 * The time the lead source recurring expense ends
 * @member {Date} end_time
 */
CreateLeadSourceRecurringExpenseRequest.prototype['end_time'] = undefined;






export default CreateLeadSourceRecurringExpenseRequest;
