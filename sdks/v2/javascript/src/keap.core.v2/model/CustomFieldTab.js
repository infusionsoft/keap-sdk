/**
 * Keap API
 * Keap Public API Documentation
 *
 * The version of the OpenAPI document: v2
 * Contact: api.keap@thryv.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CustomFieldTab model module.
 * @module keap.core.v2/model/CustomFieldTab
 */
class CustomFieldTab {
    /**
     * Constructs a new <code>CustomFieldTab</code>.
     * @alias module:keap.core.v2/model/CustomFieldTab
     */
    constructor() { 
        
        CustomFieldTab.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>CustomFieldTab</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:keap.core.v2/model/CustomFieldTab} obj Optional instance to populate.
     * @return {module:keap.core.v2/model/CustomFieldTab} The populated <code>CustomFieldTab</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CustomFieldTab();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('order')) {
                obj['order'] = ApiClient.convertToType(data['order'], 'Number');
            }
            if (data.hasOwnProperty('record_type')) {
                obj['record_type'] = ApiClient.convertToType(data['record_type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CustomFieldTab</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CustomFieldTab</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['record_type'] && !(typeof data['record_type'] === 'string' || data['record_type'] instanceof String)) {
            throw new Error("Expected the field `record_type` to be a primitive type in the JSON string but got " + data['record_type']);
        }

        return true;
    }


}



/**
 * The unique identifier of the custom field tab
 * @member {String} id
 */
CustomFieldTab.prototype['id'] = undefined;

/**
 * The name of the custom field tab
 * @member {String} name
 */
CustomFieldTab.prototype['name'] = undefined;

/**
 * The display order of the tab
 * @member {Number} order
 */
CustomFieldTab.prototype['order'] = undefined;

/**
 * The record type this tab belongs to (e.g., CONTACT, COMPANY, OPPORTUNITY, REFERRAL_PARTNER, ORDER, SUBSCRIPTION, TASK_NOTE_APPOINTMENT)
 * @member {module:keap.core.v2/model/CustomFieldTab.RecordTypeEnum} record_type
 */
CustomFieldTab.prototype['record_type'] = undefined;





/**
 * Allowed values for the <code>record_type</code> property.
 * @enum {String}
 * @readonly
 */
CustomFieldTab['RecordTypeEnum'] = {

    /**
     * value: "CONTACT"
     * @const
     */
    "CONTACT": "CONTACT",

    /**
     * value: "REFERRAL_PARTNER"
     * @const
     */
    "REFERRAL_PARTNER": "REFERRAL_PARTNER",

    /**
     * value: "OPPORTUNITY"
     * @const
     */
    "OPPORTUNITY": "OPPORTUNITY",

    /**
     * value: "TASK_NOTE_APPOINTMENT"
     * @const
     */
    "TASK_NOTE_APPOINTMENT": "TASK_NOTE_APPOINTMENT",

    /**
     * value: "COMPANY"
     * @const
     */
    "COMPANY": "COMPANY",

    /**
     * value: "ORDER"
     * @const
     */
    "ORDER": "ORDER",

    /**
     * value: "SUBSCRIPTION"
     * @const
     */
    "SUBSCRIPTION": "SUBSCRIPTION",

    /**
     * value: "unknown_default_open_api"
     * @const
     */
    "unknown_default_open_api": "unknown_default_open_api"
};


export default CustomFieldTab;
